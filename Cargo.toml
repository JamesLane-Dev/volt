[package]
name = "volt"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = ["2d"] }
rand = "0.8.5"

# Fast debug builds
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
